# Claude Code Adapter

将 Speculo 接入 Claude Code 的开箱即用粘合层。

## 接入步骤

[TODO: 详细列出复制命令、`.claude/commands/` 注册方式、验证是否生效的方法、卸载步骤。]

## 文件说明

- `CLAUDE.md.example` — 复制到项目根的 `CLAUDE.md`，仅一行透传到 `AGENTS.md`，避免双写
- `.claude/commands/` — 复制到项目根的 `.claude/commands/`，注册 `/speculo-*` 触发命令
