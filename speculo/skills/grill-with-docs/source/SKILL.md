---
name: grill-with-docs
description: 针对计划的拷问式对话，将方案与现有领域模型对照验证，打磨术语，并在决策确定后同步更新文档（CONTEXT.md、ADR）。适用于用户想结合项目术语和已有文档决策来压力测试某个方案的场景。
---

<what-to-do>

针对这个计划的每个方面不断向我提问，直到我们达成共识。沿着设计树的每条分支逐一展开，逐个解决决策之间的依赖关系。对于每个问题，请给出你的推荐答案。

每次只问一个问题，等待我对当前问题的反馈后再继续。

如果某个问题可以通过探索代码库来回答，那就直接去探索代码库。

</what-to-do>

<supporting-info>

## 领域感知

在探索代码库时，同时留意现有文档：

### 文件结构

大多数仓库只有一个上下文：

```
/
├── CONTEXT.md
├── docs/
│   └── adr/
│       ├── 0001-event-sourced-orders.md
│       └── 0002-postgres-for-write-model.md
└── src/
```

如果根目录存在 `CONTEXT-MAP.md`，说明该仓库有多个上下文。映射文件会指出每个上下文的位置：

```
/
├── CONTEXT-MAP.md
├── docs/
│   └── adr/                          ← 系统级决策
├── src/
│   ├── ordering/
│   │   ├── CONTEXT.md
│   │   └── docs/adr/                 ← 上下文特定决策
│   └── billing/
│       ├── CONTEXT.md
│       └── docs/adr/
```

按需创建文件——只在确实有内容要写时才创建。如果 `CONTEXT.md` 不存在，在第一个术语确定时创建。如果 `docs/adr/` 不存在，在第一个 ADR 需要时创建。

## 对话过程中

### 对照术语表提出质疑

当用户使用的术语与 `CONTEXT.md` 中已有的定义冲突时，立即指出。「你的术语表将『取消』定义为 X，但你现在的意思似乎是 Y——到底是哪个？」

### 打磨模糊用语

当用户使用含混或一词多义的术语时，提议一个精确的规范术语。「你说的是『账户』——你指的是 Customer 还是 User？这两个是不同的东西。」

### 讨论具体场景

当讨论领域关系时，用具体场景来压力测试。构造能探测边界情况的场景，迫使用户精确界定概念之间的边界。

### 与代码交叉验证

当用户描述某个东西如何运作时，检查代码是否一致。如果发现矛盾，直接指出：「你的代码取消的是整个订单，但你刚才说可以部分取消——哪个是对的？」

### 即时更新 CONTEXT.md

当一个术语确定后，立即更新 `CONTEXT.md`。不要攒着——在确定的当下就记录。使用 [CONTEXT-FORMAT.md](./CONTEXT-FORMAT.md) 中的格式。

`CONTEXT.md` 应该完全不涉及实现细节。不要把 `CONTEXT.md` 当作规格说明、草稿纸或实现决策的存储库。它只是一个术语表，仅此而已。

### 谨慎提议 ADR

只有同时满足以下三个条件时，才提议创建 ADR：

1. **难以逆转** —— 日后改变主意的代价不可忽略
2. **缺少上下文会令人意外** —— 未来的读者会疑惑「为什么他们要这样做？」
3. **真实权衡的结果** —— 确实存在替代方案，而你基于特定原因选择了其中一个

三个条件缺一不可，缺了任何一个就跳过 ADR。使用 [ADR-FORMAT.md](./ADR-FORMAT.md) 中的格式。

</supporting-info>
