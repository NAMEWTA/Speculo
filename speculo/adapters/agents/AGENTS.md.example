# AGENTS.md

本项目使用 **Speculo 框架** 驱动 AI Coding 工作流。

## 项目约定

- `workflows/` 是多阶段工作流入口，按 `dev` / `doc` / `ops` 分类。
- `workflows/dev/00-INDEX.md` 是开发工作流导航，暴露 `dev/01`、`dev/02`、`dev/03`、`dev/I`、`dev/H` 入口别名。
- `commands/` 是一次性命令入口，内置状态、归档和 productivity commands。
- `skills/` 是可复用原子能力目录；Speculo wrapper 位于 `skills/<name>/SKILL.md`，原始技能内容如存在则放在同目录 `source/` 下。
- skill 不直接写 `.speculo/` 或 `.status.json`，持久化由调用它的 workflow 或 command 负责。
- `.speculo/` 是状态与产物骨架，作为项目内持久化区。
- `.speculo/.config/*.md` 是项目级规则输入；执行前优先读取已激活的非 `.example` 文件。

## 默认入口

- 不确定做什么：先读 `workflows/<cat>/00-INDEX.md`，根据用户意图选择 `dev`、`doc` 或 `ops`。
- 做开发任务：读 `workflows/dev/00-INDEX.md`，按 `dev/01`、`dev/02`、`dev/03`、`dev/I`、`dev/H` 推荐下一步。
- 查看全局状态：执行 `commands/status.md`。
- 归档 completed change：执行 `commands/archive.md`，但必须先列清单并取得用户确认。

## 强制约束

- 遵守 `.speculo/.config/RULES.md`；该文件只能由用户维护，AI 不得自动修改。
- 工作流产物写入 `.speculo/<cat>/<change-name>/`，每个 change 必须有 `.status.json`。
- 更新 workflow 进度时同步维护对应 `.speculo/<cat>-status.json`。
- 不创建物理 `STATUS.json`；全局状态由 `commands/status.md` 聚合。
- 模板中的 `[TODO: ...]` 是用户填写占位符；生成产物时必须整块覆盖，不残留。
- 不修改 `skills/<name>/source/` 下的原始技能内容；需要改变行为时增强 wrapper、workflow 或 command。
