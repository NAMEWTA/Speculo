# TDD 规则

1. 从 Ticket 验收或验证矩阵选择下一条最小行为。
2. 通过稳定接缝写失败测试，并确认失败原因正确。
3. 只写使该行为通过的最小生产代码。
4. 运行定向测试；通过后重构，保持绿色。
5. 周期性运行受影响回归，完成时运行完整要求。

## 禁止

- 先写大段实现再补测试；
- Mock 被测对象内部实现；
- 断言私有调用次数而非外部效果（除非调用本身是合同）；
- 为通过测试而吞错、降低断言、跳过用例；
- 把旧失败无证据地归因于环境。
